{
  "name": "homebridge-zz-zengge-ble-strips",
  "displayName": "Zigzag Zengge BLE Strips",
  "version": "1.0.0",
  "private": true,
  "description": "Homebridge plugin to control Zengge LED strip over BLE",
  "main": "dist/index.js",
  "keywords": [
    "homebridge-plugin",
    "zengge",
    "ledstrip",
    "ble"
  ],
  "peerDependencies": {
    "homebridge": "^2.0.0-beta.0"
  },
  "engines": {
    "node": "^v18.18.0",
    "homebridge": "^1.8.0 || ^2.0.0-beta.0"
  },
  "dependencies": {
    "@abandonware/noble": "^1.9.2-25"
  },
  "devDependencies": {
    "@types/node": "^12.11.7",
    "@types/ws": "^6.0.3",
    "nodemon": "^1.19.4",
    "tslint": "^5.20.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "tsc",
    "watch": "nodemon",
    "prepublishOnly": "npm i --package-lock-only && npm audit && npm run build"
  }
}
